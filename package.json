{
  "name": "catch",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": "16.3.0"
  },
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd client && yarn add --dev && yarn && yarn build"
  },
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "got": "^11.8.2",
    "nodemon": "^2.0.12"
  }
}
